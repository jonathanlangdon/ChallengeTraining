// convert a number type to string

export const numberToString = (num: number): string => num + '';

console.log(typeof numberToString(3)); // string

/* other methods:

num.toString()
String(num)
$(num)

*/
